#include<stdio.h>
/*
int main(void) {

	int codeNumber[5];
	float prices[5];
	int quantity[5];

	int inputs = 0;
	printf("How many producst you what top enter \n");
	scanf_s("%f", &inputs);

	for (int i = 0; i >= inputs; i++) {

		printf("Enter code number \n");
		scanf_s("%d", &codeNumber);
		printf("Enter the price of the product \n");
		scanf_s("%f", &prices);
		printf("Enter how many quantity you have \n");
		scanf_s("%d", &quantity);

	}

	printf("Code Number | Product Price | Quantity | Total");

	for (int i = 0; i < inputs; i++) {

		float total = prices[i] * inputs;
		printf("Element[%d]            Element[%f]           Element[%d]        %.2f", codeNumber[i], prices[i], quantity[i], total);

	}


}

*/